[project]
name = "docxray"
version = "0.1.0"
description = "ReadOnly python-docx fork lib"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "lxml>=6.1.0",
]

[dependency-groups]
dev = [
    "types-lxml>=2026.2.16",
]

[tool.pyright]
root = ['src']
